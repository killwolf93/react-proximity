const defaultState = {name: 'Proximity Code challenge', email: 'foo@proximitycr.com'}

export const User = (state = defaultState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

